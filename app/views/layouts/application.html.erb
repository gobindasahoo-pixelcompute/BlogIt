<!DOCTYPE html>
<html>
  <head>
    <title>BlogIt</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>

  <body class="bg-gray-50 text-gray-900 font-inter">
    <%= yield %>
    <%= render "shared/bundle", entrypoint: "application" %>
  </body>
</html>
